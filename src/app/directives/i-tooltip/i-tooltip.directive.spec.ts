import { ITooltipDirective } from './i-tooltip.directive';

describe('ITooltipDirective', () => {
  it('should create an instance', () => {
    const directive = new ITooltipDirective();
    expect(directive).toBeTruthy();
  });
});
